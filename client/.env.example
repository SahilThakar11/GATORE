# Frontend Google OAuth client ID
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id

# Note:
# The current frontend code does not read VITE_API_URL.
# Development API requests are proxied through Vite from /api to http://localhost:3000.
